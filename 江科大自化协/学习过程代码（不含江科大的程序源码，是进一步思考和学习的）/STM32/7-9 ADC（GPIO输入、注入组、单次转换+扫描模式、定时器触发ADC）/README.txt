实现效果：
       定时器作为ADC的外部触发源，启动ADC的注入组转换

各个模块：
       TIM1_TRGO：配置TIM1的主模式输出TRGO，定时周期为1s

       TIM2_CC1：配置TIM2的输出比较OC结构体，定时周期为5s。不需要配置GPIO输出PWM，我们只需要

       AD：外部触发ADC后，注入组转换；转换结束后，触发ADC全局中断；
               在中断中将JDRx的数据读取出来，并使用LED翻转电平来指示运行状态。